# Reference this view as dbt_sleon__my_first_dbt_model
schema: DBT_SLEON
table_name: MY_FIRST_DBT_MODEL

dimensions:
  id:
    sql: '"ID"'
    format: ID
    primary_key: true

measures:
  count:
    aggregate_type: count
